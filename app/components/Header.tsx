import { FC } from "react";

const Header: FC = () => {
  return (
    <header className="relative">
      <div className="absolute whitespace-pre-wrap">TAMGOCHI PROJECT</div>
    </header>
  );
};

export default Header;
