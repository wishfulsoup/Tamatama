import React, { FC } from "react";
import Header from "../components/Header";

const Dressroom: FC = () => {
  return (
    <section className="bg-theme h-[100vh]">
      <Header />
    </section>
  );
};

export default Dressroom;
